# DSN PostgreSQL — default sudah cocok dengan docker-compose.yml di root repo
DB_DSN=host=localhost user=minishop password=minishop dbname=minishop port=5432 sslmode=disable

# Port HTTP server
PORT=8080
